<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xiaoyu_j.mapper.MessageMapper">

    <!-- 分页查询两个用户之间的聊天记录 -->
    <select id="selectChatHistory" resultType="com.xiaoyu.entity.MessagePO">
        SELECT * FROM messages 
        WHERE deleted = 0
        AND ((from_id = #{userId1} AND to_id = #{userId2}) 
             OR (from_id = #{userId2} AND to_id = #{userId1}))
        ORDER BY created_at DESC
    </select>

    <!-- 标记消息为已读 -->
    <update id="markMessagesAsRead">
        UPDATE messages 
        SET is_read = 1, updated_at = NOW()
        WHERE from_id = #{fromId} 
        AND to_id = #{toId} 
        AND is_read = 0 
        AND deleted = 0
    </update>

    <!-- 获取未读消息数量 -->
    <select id="getUnreadCount" resultType="java.lang.Long">
        SELECT COUNT(*) 
        FROM messages 
        WHERE to_id = #{toId} 
        AND is_read = 0 
        AND deleted = 0
    </select>

</mapper>
